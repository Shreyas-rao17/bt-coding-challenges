# Challenge 3: Calculate discount on total amount

Accept total amount and discount percentage, compute discount and final amount.

Formula: discount = (total * percent) / 100, final = total - discount

Handles invalid inputs (negative values, percent > 100).