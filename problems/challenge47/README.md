# Challenge 47: Level 0: Write a program to accept n and store the elements into the array of size n.

Accept n and store n integers into an array.

## Requirements
- Input: n (positive integer), then n integers
- Output: The array
- Validation: n > 0, elements integers