# Coding Challenge 1: Program to find the sum and average of two variables

Write a program that accepts two variables (numbers), calculates their sum and average, and displays the results.

## Requirements
- Accept two numeric inputs
- Compute sum and average
- Display the results clearly
- Handle invalid inputs gracefully

## Solution Approach
- Use a function to calculate sum and average
- Handle floating point inputs
- Provide clear output formatting