- Static variables are shared across all method calls.

- Static methods can access and modify static variables.

- Initialization logic can be placed inside a getter method.

- The cache capacity is initialized only once and reused later.

- This lab demonstrates controlled initialization using static members.