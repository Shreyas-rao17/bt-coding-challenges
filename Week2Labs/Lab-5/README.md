- Swapping variables inside a method does not affect the original variables.

- Python passes values to methods, not the actual variables.

- To update values, the swapped values must be returned.

- The caller must assign the returned values back to the variables.

- This lab shows the importance of understanding parameter passing.